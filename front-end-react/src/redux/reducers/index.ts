import { combineReducers } from 'redux'
import Authentication from './Authentication'
import InitializeObj from './EventDetails'

export default combineReducers({
    Authentication,
    InitializeObj,
})